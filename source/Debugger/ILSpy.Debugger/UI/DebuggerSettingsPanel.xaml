<UserControl x:Class="ICSharpCode.ILSpy.Options.DebuggerSettingsPanel"
             x:ClassModifier="internal"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
	<Grid>
		<StackPanel Margin="10">
			<CheckBox IsChecked="{Binding ShowWarnings}">显示警告信息</CheckBox>
			<CheckBox IsChecked="{Binding AskForArguments}">在启动进程前提示输入参数和工作目录</CheckBox>
			<CheckBox IsChecked="{Binding ShowAllBookmarks}">在断点窗口显示所有书签</CheckBox>
      <CheckBox IsChecked="{Binding BreakAtBeginning}">启动或附加进程后先中断</CheckBox>
    </StackPanel>
	</Grid>
</UserControl>